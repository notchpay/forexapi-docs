import theme from "@nuxt/content-theme-docs";

export default theme({
  docs: {
    primaryColor: "#f7c243",
  },
  server: {
    port: 30007, // default: 3000
    host: "localhost", // default: localhost
  },
});
